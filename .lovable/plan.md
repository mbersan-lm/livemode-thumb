## Plan: COMPLETED — `competicao` and `modelo` Parameters

Edge function `generate-ao-vivo` updated to support `competicao` and `modelo` parameters.

- `competicao`: selects logo overlay (conference vs europa) without rendering text
- `modelo: "sem narracao"`: skips panels overlay, adds Som Ambiente overlay at the end
